name: ENV Variable
on: push
env: 
  WF_ENV: Available to all jobs

jobs:
  log-env:
    runs-on: ubuntu-latest
    steps:
      - name: Log environment variables
        run: |
          echo "WF ENV ${WF_ENV}"
    
